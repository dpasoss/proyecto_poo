export class User {
    constructor(nombre, correo, contraseña, rol, datosCandidato, datosEmpleador) {
      this.nombre = nombre;
      this.correo = correo;
      this.contraseña = contraseña;
      this.rol = rol;
      this.datosCandidato = datosCandidato;
      this.datosEmpleador = datosEmpleador
    }
}
